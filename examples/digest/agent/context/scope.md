# Scope

This agent turns accumulated notes into a weekly written summary for a human to read.

It reads `knowledge/notes/` and writes to `work/reports/`. It reaches no outside system at
all — it has no MCP servers configured and therefore no way to contact anything.
